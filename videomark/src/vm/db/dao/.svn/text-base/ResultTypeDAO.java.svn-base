package vm.db.dao;

public class ResultTypeDAO {
	/**
	 * 目前只有两个标注结果类型：视频和图像
	 * 不需要单独提供DAO接口。只在需要的时候往数据库中插入记录即可。
	 * 
	 * insert into resulttype values(uuid(), ctime, utime, '', 1, 'video');
	 * insert into resulttype values(uuid(), ctime, utime, '', 1, 'image');
	 */
}
